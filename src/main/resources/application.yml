spring:
  application:
    name: assistenciawebsistema

  datasource:
    url: jdbc:mysql://localhost:3307/assistenciaweb
    username: root
    password: 08901
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

